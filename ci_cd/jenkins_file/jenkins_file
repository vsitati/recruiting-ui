pipeline {
   agent {
        node {
            label 'RecruitingQA'
        }
    }

   stages {
      stage('Create Python Virtual Env and install libraries') {
         steps {
          powershell script: "ci_cd\\setup\\install_libraries.ps1"
         }
      }
   }
}