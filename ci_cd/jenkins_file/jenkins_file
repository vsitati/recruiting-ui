pipeline {
   agent {
        node {
            label 'RecruitingQA'
            customWorkspace 'workspace\\recruiting_qa'
        }
    }
    environment {
    JenkinsNode = "RecruitingQA"
    Workspace= "recruiting_qa_${env.BRANCH_NAME}"
    }

   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
         }
      }
   }
}